# verify.sh setting: nothing is uploaded and nothing is produced — the check drives
# the running service, not a transfer. The harness deletes these paths around the
# run, so they are a directory of its own rather than the volume root.
input=filegator
output=filegator
