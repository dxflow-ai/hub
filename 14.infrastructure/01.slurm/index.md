---
title: Slurm
description: Workload manager and job scheduler for HPC clusters
navigation:
    icon: i-diphyx:slurm
---

Slurm is an open-source workload manager and job scheduler for Linux clusters, handling queuing, allocation, and execution of compute jobs.

## Coming soon

This workflow is in preparation — its image and definition are being built and end-to-end verified. Once it ships, this page gains **Usage** and **Configuration** sections and it deploys by name:

```bash
dxflow workflow create --identity slurm hub://slurm
```

Meanwhile, browse the published workflows with `dxflow workflow hub search`, or deploy any workflow already in the catalog — see [How it works](/hub/getting-started/how).

Want it sooner, or want to build it? Say so in the [community issue tracker](https://github.com/dxflow-ai/community/issues), or open a pull request to [dxflow-ai/hub](https://github.com/dxflow-ai/hub) following the structure of a published entry.
