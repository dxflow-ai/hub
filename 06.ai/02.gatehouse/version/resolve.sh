# Gatehouse is Open WebUI re-published from the upstream image, which tags every
# release as v<version> — the plain variant, without the bundled model server.

echo "VERSION=$(github_release open-webui/open-webui)"
