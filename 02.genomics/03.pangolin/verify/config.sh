# verify.sh setting: input volume host dir (uploaded to, before start)
input=input

# verify.sh setting: output volume host dir (checked by expect_output)
output=output

# verify.sh setting: pangolin downloads data and aligns, so allow more time
timeout=600
